这是一个基于CEF3（嵌入式 Chromium 框架）二次开发的库，使用BSD许可证开源。

CEF3是基于C++的，大量使用了Class，这意味着C、易语言不能很方便地使用，此库是作为一个“中间层”将复杂的Class隐藏，从而给上层提供使用WINAPI调用约定的接口，这就意味着这些函数能够很方便地被C、易语言所调用，摆脱了class的烦恼，体验和Win32API编程一样的感觉。（当然我并不是说Class不好）

这是在cefsimple的基础上进行开发的，这是一个Visual Studio 2013工程（并且必须用Release模式编译）。CEF的版本是 cef_binary_3.2062.1930_windows32 理论上兼容更新的版本。

自带了编译此库所需的CEF3 Lib，但是没有带CEF3工作的DLL，可以下载CEF3源码编译得到。

我写的代码逻辑并不明了，注释并不丰富，性能并不好，如果不是有人总是鼓动我做我也就不打算发了，所以请各路前辈轻喷。（如果路过看到的话）

另外这是我第一次用Git。。。

这是使用Google机器翻译后的英文版本：
This is the English version after using the Google machine translation:

This is based on CEF3 (Chromium Embedded Framework) secondary development library, using the open source BSD license.

CEF3 is based on C++, the extensive use of the Class, which means C, easy language can not easily use this library as a "middle-tier" Class complex hidden, giving the upper layer provides an interface using the WINAPI calling convention, This means that these functions can easily be called by C, easy language, get rid of the class of troubles, and Win32API programming experience the same feeling.(Of course, I'm not saying bad Class)

It is developed on the basis of cefsimple This is a Visual Studio 2013 project (And must be compiled using the Release mode). CEF cef_binary_3.2062.1930_windows32 theoretically compatible version is an updated version.

Comes required to compile this library CEF3 Lib, but did not take CEF3 DLL work, you can download the source code to compile CEF3 get.

I wrote the code logic is not clear, the comment is not rich, the performance is not good, so the brightest light spray predecessors. (If you pass by to see it)

Also this is the first time I used Git. . .